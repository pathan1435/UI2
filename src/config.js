export const configFun = (accessKey, secretKey, region) => {
  return {
    ACCESS_KEY_ID: "",        // Use the parameter accessKey here
    SECRET_KEY: "",           // Use the parameter secretKey here
    REGION: ""                  // Use the parameter region here
  };
};

